collaboration ModSL3 { 
	:processor->:lexer.tokenStream(); 
	:processor->:parser.diagram():graph; 
	:processor->:layoutVisitor.accept(graph); 
	:layoutVisitor->:graph.getNodes():node; 
	:layoutVisitor->:collabNodeLayout.apply(node); 
	:layoutVisitor->:circleLayout.apply(graph); 
	:layoutVisitor->:fruchtermanRheingoldLayout.apply(graph); 
	:processor->:graph.rescale(); 
	:processor->:stringTemplateVisitor.accept(graph);
}
