group modsl;

graph(name, statements) ::= <<
graph <name> {
	<statements; separator="\n">
}
>>

statement(text) ::= "<text>"

node(name) ::= "node(<name>) "

edge(name) ::= "edge(<name>) "

attribute(key, value) ::= "attribute(<key>, <value>) "
